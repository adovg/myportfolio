import React from "react";
import styles from "./Footer.module.scss"; // Assuming you have a CSS module for styling

const Footer = () => {
  return <footer className={styles.footer}>Footer here</footer>;
};

export default Footer;
